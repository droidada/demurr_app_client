import React from 'react';
import { ReactComponent as MyLoader } from '../assets/loader.svg';

const Loader = () => {
  return (
    <div>
      <MyLoader />
    </div>
  );
};

export default Loader;
