import client from '../utils/client';
import { useHistory } from 'react-router-dom';

export const useEditTarrif = () => {
  const history = useHistory();

  return {}

};

export const useFetchTarrif = () => {
    
    return { data: {}, status: 'success'}
};
   